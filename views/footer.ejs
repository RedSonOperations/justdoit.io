
</body>
<footer id="footer" style="color: whitesmoke">
    © 2023 RedSonOperations
  </footer>
  
  <script>
    var footer = document.getElementById("footer");
    var companyName = document.getElementById("companyName");
    var color = 0;
    var increment = 1;
    setInterval(function() {
      color += increment;
      if (color >= 100) {
        increment = -1;
      } else if (color <= 0) {
        increment = 1;
      }
      var r = 255 - Math.floor(color * 2.55);
      var g = 255 - Math.floor(color * 2.55);
      var b = 255 - Math.floor(color * 2.55);
      footer.style.color = "rgb(" + r + ", " + g + ", " + b + ")";
      if (color <= 50) {
        companyName.style.color = "black";
      } else {
        companyName.style.color = "whitesmoke";
      }
    }, 40);
  </script>
</html>